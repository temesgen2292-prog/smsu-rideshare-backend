<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">
      <img src="/img/smsu-mustang-logo.png" alt="Mustang RideShare Logo" width="30" height="30" class="d-inline-block align-text-top me-2">
      Mustang RideShare
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <a class="nav-link" href="/">Home</a>
        </li>

        <% if (isLoggedIn) { %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Welcome, <%= user.name %>!
            </a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
              <% if (user.role === 'admin') { %>
                <li><a class="dropdown-item" href="/dashboard/admin">Admin Dashboard</a></li>
              <% } else if (user.role === 'driver') { %>
                <li><a class="dropdown-item" href="/dashboard/driver">Driver Dashboard</a></li>
              <% } else { %>
                <li><a class="dropdown-item" href="/dashboard/student">Student Dashboard</a></li>
              <% } %>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item text-danger" href="/logout">Logout</a></li>
            </ul>
          </li>
        <% } else { %>
          <li class="nav-item">
            <a class="nav-link" href="/login">Login</a>
          </li>
          <li class="nav-item">
            <a class="nav-link btn btn-sm btn-warning text-dark ms-2" href="/register">Register</a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>